import { ListType } from "definition";

const pages = {
  menu: [
    {
      title: "Past",
      type: ListType.Past,
    },
    {
      title: "Next",
      type: ListType.NEXT,
    },
  ],
};

export default pages;
